# ============================================================================
# ASE Foundations — Day 9 CI/CD Exercise
# GitHub Actions Workflow: Build, Test, and Deploy to Railway
# ============================================================================
#
# YOUR TASK:
# This workflow file is INCOMPLETE. You need to fill in the missing steps
# marked with TODO comments. The pipeline should:
#
#   1. Run backend tests (Python/Django)
#   2. Build Docker images for backend and frontend
#   3. Push images to Docker Hub
#   4. Trigger a redeploy on Railway
#
# SETUP REQUIRED (GitHub Repository Secrets):
#   - DOCKER_USERNAME: Your Docker Hub username
#   - DOCKER_PASSWORD: Your Docker Hub password/access token
#   - RAILWAY_TOKEN: Your Railway API token (Dashboard → Account → Tokens)
#
# ============================================================================

name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── Job 1: Run Backend Tests ──
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt

      # TODO: Add a step to run Django tests
      # Hint: cd backend && python manage.py test
      - name: Run tests
        run: |
          echo "TODO: Run backend tests here"

  # ── Job 2: Build and Push Docker Images ──
  build-and-push:
    runs-on: ubuntu-latest
    needs: test-backend
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # TODO: Log in to Docker Hub
      # Hint: Use the docker/login-action@v3 action
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Login to Docker Hub
        run: |
          echo "TODO: Login to Docker Hub"

      # TODO: Build and push the backend Docker image
      # Hint:
      #   docker build -t ${{ secrets.DOCKER_USERNAME }}/taskmanager-backend:latest ./backend
      #   docker push ${{ secrets.DOCKER_USERNAME }}/taskmanager-backend:latest
      - name: Build and push backend image
        run: |
          echo "TODO: Build and push backend image"

      # TODO: Build and push the frontend Docker image
      # Hint: Don't forget the --build-arg for REACT_APP_API_URL
      #   docker build -t ${{ secrets.DOCKER_USERNAME }}/taskmanager-frontend:latest \
      #     --build-arg REACT_APP_API_URL=/api ./frontend
      #   docker push ${{ secrets.DOCKER_USERNAME }}/taskmanager-frontend:latest
      - name: Build and push frontend image
        run: |
          echo "TODO: Build and push frontend image"

  # ── Job 3: Deploy to Railway ──
  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # TODO: Install the Railway CLI and trigger a redeploy
      # Hint: Install the CLI, then use it to redeploy your services.
      #   npm install -g @railway/cli
      #
      #   The Railway CLI uses the RAILWAY_TOKEN env variable for auth.
      #   You can trigger a redeploy with:
      #     railway redeploy --service <SERVICE_ID> --yes
      #
      #   You'll need to find your service IDs from the Railway dashboard
      #   (click a service → Settings → Service ID)
      #
      #   Set the RAILWAY_TOKEN as an environment variable:
      #   env:
      #     RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      - name: Deploy to Railway
        run: |
          echo "TODO: Install Railway CLI and trigger redeploy"
